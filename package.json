{
  "name": "seuservico-app",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo do projeto SeuServiço",
  "scripts": {
    "test": "npm --prefix backend run test:integration",
    "build": "npm --prefix backend run build && npm --prefix admin-panel run build && npm --prefix mobile run build"
  },
  "license": "ISC",
  "type": "commonjs"
}
